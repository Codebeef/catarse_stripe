= javascript_include_tag 'catarse_stripe'

h3= t('projects.contributors.review.international.section_title')

#catarse_stripe_form
  = form_tag charge_stripe_path(params[:id]) do
    .clearfix
    .bootstrap-twitter
      = submit_tag t('projects.contributors.review.international.button'), :class => 'btn btn-primary btn-large'
